class Puppet {
  constructor(productId, name, desc, price, image, url) {
    this.productId = productId;
    this.name = name;
    this.desc = desc;
    this.price = price;
    this.image = image;
    this.url = url;
  }
}

export default Puppet;
