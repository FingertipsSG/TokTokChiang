class Frame {
  constructor(productId, name, desc, price, image) {
    this.productId = productId;
    this.name = name;
    this.desc = desc;
    this.price = price;
    this.image = image;
  }
}

export default Frame;
